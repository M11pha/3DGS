# Shape of Motion:  4D Reconstruction from a Single Video

## Train

```bash
initialize_and_checkpoint_model(
        cfg,
        train_dataset,
        device,
        ckpt_path,
        vis=cfg.vis_debug,
        port=cfg.port,
    )
```

